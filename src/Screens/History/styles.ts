import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    flatList: {
        backgroundColor: "#ffffff",
        height: "100%",
        paddingTop: 8
    },
    flatlistContent: {
        paddingBottom: 125
    }
})